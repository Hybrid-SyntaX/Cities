@model IEnumerable<Cities.Models.City>

@{
    ViewData["Title"] = "List of cities";
}

<h2>@ViewData["Title"]</h2>

<ul>
    @foreach (var item in Model)
    {
        <li>
            @Html.DisplayFor(modelItem => item.Name)
        </li>
    }
</ul>
